let altura_cm = 173;
let altura_m = 1.73;
let peso_kg = 57.6;

let altura_round_m = Math.ceil(altura_m)
console.log(altura_round_m)

let peso_round_kg = Math.floor(peso_kg)
console.log(peso_round_kg)

let max_value_JS = Number.MAX_VALUE
if (max_value_JS === max_value_JS +1) {
    console.log("El maximo valor que se puede obtener en JavaScript mas uno es igual al valor maximo que se puede obtener en JavaScript")
}
